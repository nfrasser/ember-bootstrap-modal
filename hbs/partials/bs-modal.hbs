{{!-- Modal --}}
{{!
	When called from a BSModalRoute, will be automatically placed inside
	an BSModalView
}}
<div class="modal-dialog">
	<div class="modal-content">
		{{#if controller.hasBSModalHeader}}
			<div class="modal-header">
				{{view view.close}}
				{{outlet "modal-header"}}
			</div>
		{{/if}}

		<div class="modal-body">
			{{#unless controller.hasBSModalHeader}}{{view view.close}}{{/unless}}
			{{outlet "modal-body"}}
		</div>

		{{#if controller.hasBSModalFooter}}
			<div class="modal-footer">
				{{outlet "modal-footer"}}
			</div>
		{{/if}}
	</div>{{!-- /.modal-content --}}
</div>{{!-- /.modal-dialog --}}
